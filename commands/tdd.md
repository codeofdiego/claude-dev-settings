# Switch to TDD workflow mode

Then use the TDD workflow defined in @docs/workflows/tdd_workflow.md to work on $ARGUMENT and use subagent to parallelize work when it is possible.

The workflow hook will now use TDD mode for reminders.
EOF < /dev/null