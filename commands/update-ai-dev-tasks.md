# Update AI Dev Tasks Command

This command updates the ai-dev-tasks repository to get the latest workflow files.

Run these commands to update:
```bash
cd ~/.claude/docs/ai-dev-tasks
git pull origin main
```

This ensures you always have the most current version of the structured development workflow.